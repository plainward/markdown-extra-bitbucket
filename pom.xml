<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.plainward.bitbucket</groupId>
    <artifactId>markdown-extra</artifactId>
    <version>1.0.1</version>
    <packaging>atlassian-plugin</packaging>

    <name>Markdown Extra for Bitbucket (MarkdownX)</name>
    <description>Enhanced Markdown rendering for Bitbucket Data Center: Mermaid diagrams, PlantUML, and LaTeX math.</description>
    <organization>
        <name>Plainward Software</name>
        <url>https://plainward.com</url>
    </organization>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <url>https://github.com/plainward/markdown-extra-bitbucket</url>
        <connection>scm:git:git://github.com/plainward/markdown-extra-bitbucket.git</connection>
        <developerConnection>scm:git:git@github.com:plainward/markdown-extra-bitbucket.git</developerConnection>
    </scm>

    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/plainward/markdown-extra-bitbucket/issues</url>
    </issueManagement>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>

        <!--
            Build target = Bitbucket 8.19.0 (LTS). Plugin verified to run on:
              - BB 8.19.28 (default JAR, javax.*)
              - BB 9.6.3   (default JAR, javax.*)
              - BB 10.2.1  (-bb10 JAR, jakarta.*, generated via Tomcat jakartaee-migration)
            Marketplace compatibility ranges (set per artifact in Marketplace UI):
              - default JAR: BB 8.0.0  – 9.99.99
              - -bb10 JAR : BB 10.0.0 – open
        -->
        <bitbucket.version>8.19.0</bitbucket.version>
        <amps.version>9.1.1</amps.version>

        <sal.version>4.4.3</sal.version>
        <plantuml.version>1.2024.8</plantuml.version>
        <gson.version>2.10.1</gson.version>
        <javax.inject.version>1</javax.inject.version>
        <javax.servlet.version>3.1.0</javax.servlet.version>
        <spring.version>5.3.29</spring.version>
        <atlassian.spring.scanner.version>2.2.4</atlassian.spring.scanner.version>
        <template.renderer.version>4.1.1</template.renderer.version>

        <node.version>v20.11.0</node.version>
        <npm.version>10.2.4</npm.version>
        <frontend.maven.plugin.version>1.15.0</frontend.maven.plugin.version>

        <atlassian.plugin.key>com.plainward.bitbucket.markdown-extra</atlassian.plugin.key>
        <frontend.skip>false</frontend.skip>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.atlassian.bitbucket.server</groupId>
                <artifactId>bitbucket-parent</artifactId>
                <version>${bitbucket.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Bitbucket API -->
        <dependency>
            <groupId>com.atlassian.bitbucket.server</groupId>
            <artifactId>bitbucket-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.atlassian.bitbucket.server</groupId>
            <artifactId>bitbucket-spi</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- SAL (Shared Application Layer) -->
        <dependency>
            <groupId>com.atlassian.sal</groupId>
            <artifactId>sal-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Template Renderer (Velocity) -->
        <dependency>
            <groupId>com.atlassian.templaterenderer</groupId>
            <artifactId>atlassian-template-renderer-api</artifactId>
            <version>${template.renderer.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Scanner -->
        <dependency>
            <groupId>com.atlassian.plugin</groupId>
            <artifactId>atlassian-spring-scanner-annotation</artifactId>
            <version>${atlassian.spring.scanner.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Servlet -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>${javax.servlet.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Inject -->
        <dependency>
            <groupId>javax.inject</groupId>
            <artifactId>javax.inject</artifactId>
            <version>${javax.inject.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- JAX-RS -->
        <dependency>
            <groupId>javax.ws.rs</groupId>
            <artifactId>jsr311-api</artifactId>
            <version>1.1.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- JAXB (needed for Java 11+) -->
        <dependency>
            <groupId>javax.xml.bind</groupId>
            <artifactId>jaxb-api</artifactId>
            <version>2.3.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- PlantUML (bundled) -->
        <dependency>
            <groupId>net.sourceforge.plantuml</groupId>
            <artifactId>plantuml</artifactId>
            <version>${plantuml.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- OSGi -->
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>osgi.core</artifactId>
            <version>8.0.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- Gson (provided by platform) -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Frontend build -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>${frontend.maven.plugin.version}</version>
                <configuration>
                    <workingDirectory>${project.basedir}/frontend</workingDirectory>
                    <nodeVersion>${node.version}</nodeVersion>
                    <npmVersion>${npm.version}</npmVersion>
                    <skip>${frontend.skip}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals><goal>install-node-and-npm</goal></goals>
                        <phase>initialize</phase>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <goals><goal>npm</goal></goals>
                        <phase>initialize</phase>
                        <configuration>
                            <arguments>install</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-build</id>
                        <goals><goal>npm</goal></goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Atlassian AMPS -->
            <plugin>
                <groupId>com.atlassian.maven.plugins</groupId>
                <artifactId>bitbucket-maven-plugin</artifactId>
                <version>${amps.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <productVersion>${bitbucket.version}</productVersion>
                    <productDataVersion>${bitbucket.version}</productDataVersion>
                    <enableQuickReload>true</enableQuickReload>
                    <allowGoogleTracking>false</allowGoogleTracking>
                    <jvmArgs>-Xms512m -Xmx2g</jvmArgs>
                    <instructions>
                        <Atlassian-Plugin-Key>${atlassian.plugin.key}</Atlassian-Plugin-Key>
                        <Spring-Context>*</Spring-Context>
                        <Import-Package>
                            com.plainward.bitbucket.markdownx.*,
                            com.atlassian.*;resolution:=optional,
                            javax.*;resolution:=optional,
                            org.springframework.*;resolution:=optional,
                            org.eclipse.*;resolution:=optional,
                            org.osgi.*;resolution:=optional,
                            *;resolution:=optional
                        </Import-Package>
                        <Export-Package/>
                    </instructions>
                </configuration>
            </plugin>

            <!-- Spring Scanner -->
            <plugin>
                <groupId>com.atlassian.plugin</groupId>
                <artifactId>atlassian-spring-scanner-maven-plugin</artifactId>
                <version>${atlassian.spring.scanner.version}</version>
                <executions>
                    <execution>
                        <goals><goal>atlassian-spring-scanner</goal></goals>
                        <phase>process-classes</phase>
                    </execution>
                </executions>
            </plugin>

            <!-- BB10 jakarta JAR — runs after package, rewrites javax.* -> jakarta.* -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>generate-bb10-jar</id>
                        <phase>package</phase>
                        <goals><goal>java</goal></goals>
                        <configuration>
                            <mainClass>org.apache.tomcat.jakartaee.MigrationCLI</mainClass>
                            <includePluginDependencies>true</includePluginDependencies>
                            <classpathScope>compile</classpathScope>
                            <arguments>
                                <argument>-profile=EE</argument>
                                <argument>${project.build.directory}/${project.build.finalName}.jar</argument>
                                <argument>${project.build.directory}/${project.build.finalName}-bb10.jar</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.apache.tomcat</groupId>
                        <artifactId>jakartaee-migration</artifactId>
                        <version>1.0.8</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>atlassian-public</id>
            <url>https://packages.atlassian.com/maven-external/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>atlassian-public</id>
            <url>https://packages.atlassian.com/maven-external/</url>
        </pluginRepository>
    </pluginRepositories>
</project>
