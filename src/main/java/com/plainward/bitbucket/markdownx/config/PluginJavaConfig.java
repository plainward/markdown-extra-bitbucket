package com.plainward.bitbucket.markdownx.config;

/**
 * Plugin configuration marker.
 * OSGi component imports are handled via @ComponentImport annotations
 * on constructor parameters in service classes (atlassian-spring-scanner 2.x).
 */
public final class PluginJavaConfig {
    private PluginJavaConfig() {}
}
